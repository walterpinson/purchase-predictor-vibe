# Azure ML Configuration
# Copy this file to .env.local and fill in your actual values
# Never commit .env.local to version control!

# Required: Azure Subscription Details
AZURE_SUBSCRIPTION_ID=your-subscription-id-here
AZURE_RESOURCE_GROUP=your-resource-group-name
AZURE_WORKSPACE_NAME=your-azure-ml-workspace-name

# Required: Azure Region
AZURE_LOCATION=eastus

# Optional: MLFlow Configuration
MLFLOW_TRACKING_URI=file:./mlruns

# Optional: Model Configuration
MODEL_NAME=purchase_predictor_model
EXPERIMENT_NAME=purchase_predictor

# Optional: Deployment Configuration
ENDPOINT_NAME=purchase-predictor-endpoint
DEPLOYMENT_NAME=purchase-predictor-deployment

# Optional: Debug and Development
DEBUG_MODE=false
VERBOSE_LOGGING=false

# Instructions:
# 1. Copy this file: cp .env.local.example .env.local
# 2. Replace 'your-*-here' values with your actual Azure details
# 3. Get your subscription ID: az account show --query id -o tsv
# 4. Create or use existing resource group and Azure ML workspace
# 5. Choose an Azure region (eastus, westus2, etc.)